"""Thin, opt-in framework adapters. The core never imports from here.

pytest_plugin.py is registered via the pytest11 entry point in pyproject.toml
(active once the package is installed); see DESIGN.md §5.1.
"""
